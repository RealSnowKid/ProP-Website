<?php


	// require 'vendor\phpmailer\phpmailer\PHPMailerAutoload.php';
	// require_once 'formActions.php';
	// $userEmail = $_SESSION['email'];

	// //


	// $mail = new PHPMailer;

	// //$mail->SMTPDebug = 3;                               // Enable verbose debug output

	// $mail->isSMTP();                                      // Set mailer to use SMTP
	// $mail->Host = 'smtp.gmail.com';  // Specify main and backup SMTP servers
	// $mail->SMTPAuth = true;                               // Enable SMTP authentication
	// $mail->Username = 'prop2020mail@gmail.com';                 // SMTP username
	// $mail->Password = 'Prop1234';                           // SMTP password
	// $mail->SMTPSecure = 'tls';                            // Enable TLS encryption, `ssl` also accepted
	// $mail->Port = 587;                                    // TCP port to connect to

	// $mail->setFrom('prop2020mail@gmail.com', 'Music Festival 2020'); # Sender
	// $mail->addAddress($userEmail);               # Recepient

	// // $mail->addAttachment('/var/tmp/file.tar.gz');         // Add attachments
	// // $mail->addAttachment('/tmp/image.jpg', 'new.jpg');    // Optional name
	// $mail->isHTML(true);                                  // Set email format to HTML

	// $mail->Subject = 'Your Tickets';
	// $mail->Body    = "Dear ".$userEmail.", <br> here are your tickets for the Music Festival 2020. ";
	// // $mail->AltBody = 'This is the body in plain text for non-HTML mail clients';

	// if(!$mail->send()) {
	//     echo 'Message could not be sent.';
	//     echo 'Mailer Error: ' . $mail->ErrorInfo;
	// } else {
	//     echo 'Message has been sent';
	//     header('Location: profile.php');
	// }

	// mail('403318@student.fontys.nl)', 'Test email', 'Testing to see if mailer works.');


?>